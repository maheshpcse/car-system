name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Lint & type-check
        run: |
          npm run lint
          npm run typecheck

      # `vite.config.ts` derives the base path from GITHUB_REPOSITORY, so project
      # pages (/<repo>/) and user/org pages (<user>.github.io) both work untouched.
      # Production API URL comes from repository variables (Railway public URL).
      - name: Build
        env:
          VITE_APP_ENVIRONMENT: production
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
          VITE_ASSET_BASE_URL: ${{ vars.VITE_ASSET_BASE_URL }}
          VITE_DEMO_MODE: ${{ vars.VITE_DEMO_MODE || 'true' }}
          VITE_REPOSITORY_URL: ${{ vars.VITE_REPOSITORY_URL || 'https://github.com/maheshpcse/car-system' }}
        run: npm run build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
